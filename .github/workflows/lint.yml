name: Lint

on:
  push:
  workflow_call:
    inputs:
      should_skip:
        required: false
        default: false
        type: boolean

jobs:
  lint:
    name: Lint
    if: inputs.should_skip != 'true'
    runs-on: ubuntu-latest
    steps:
      - run: exit 0
